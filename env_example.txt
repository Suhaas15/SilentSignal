# SilentSignal Environment Configuration
# Copy this file to .env and fill in your values

# NVIDIA NIM Configuration
# Default to NVIDIA hosted endpoint (set API key from build.nvidia.com)
NIM_BASE_URL=https://integrate.api.nvidia.com/v1
NIM_API_KEY=replace_me
NIM_MODEL=nvidia/nvidia-nemotron-nano-9b-v2
# Enable OpenAI SDK path for NVIDIA Integrate (1 to enable)
NIM_USE_OPENAI_SDK=0
# Reasoning token controls (used when NIM_USE_OPENAI_SDK=1 and model supports reasoning)
NIM_REASONING_MIN=1024
NIM_REASONING_MAX=2048

# Twilio Configuration (for WhatsApp integration)
TWILIO_ACCOUNT_SID=replace_me
TWILIO_AUTH_TOKEN=replace_me

# SilentSignal Configuration
SILENTSIGNAL_ALLOW_PERSIST=0
SILENTSIGNAL_DEBUG=False
SILENTSIGNAL_MAX_CONVERSATION_LENGTH=10000

# Optional: Enable debug mode
DEBUG=False

# Optional: Set analysis timeout (seconds)
ANALYSIS_TIMEOUT=30

# Email Alerts (Gmail SMTP)
EMAIL_ALERTS=0
EMAIL_METHOD=gmail
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=
EMAIL_TO=
# Minimum seconds between alerts per sender (rate limit)
ALERT_MIN_INTERVAL_SECONDS=60